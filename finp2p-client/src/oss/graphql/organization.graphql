query GetOrganizations($filter: [Filter!]) {
    organizations(filter: $filter) {
        nodes {
            id
            clusterId
            name
            finId
            types
        }
    }
}
